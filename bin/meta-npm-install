#!/usr/bin/env node

const debug = require('debug')('meta-npm-install');
const metaLoop = require('meta-loop');

if (process.argv[2] === '--help') {
  return console.log(`\n  usage:\n\n    meta npm install\n`);
}

metaLoop('npm install');